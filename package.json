{
  "name": "whatsapp-electron",
  "version": "1.0.1",
  "description": "DagMoller's WhatsApp Electron App",
  "main": "./src/index.js",
  "scripts": {
    "start": "electron ./src",
    "clean": "rm -rf ./dist",
    "pack": "electron-builder --dir"
  },
  "author": "Diego Aguirre (DagMoller)",
  "license": "ISC",
  "devDependencies": {
    "electron": "^24.8.5",
    "electron-builder": "^24.6.4"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.44",
    "electron-is-dev": "^2.0.0",
    "electron-store": "^8.1.0"
  },
  "build": {
    "linux": {
      "asarUnpack": [
        "**/node_modules/sharp/**",
        "**/assets/*.svg",
        "**/assets/*.png"
      ]
    }
  }
}
